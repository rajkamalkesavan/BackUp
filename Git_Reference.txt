Git Commands:
-------------

while doing rebase concepts

git push -- force-with-lease


git push --set-upstream origin feature/RBDIG-11696

git push --set-upstream origin feature/RBDIG-15695

git branch feature/RBDIG-11696 -u origin/feature/RBDIG-11696


git config -l  - to check config details.


git config --system core.longpaths   - File name too long need to set this command in Git Bash

 git config --global core.longpaths true
 
 git clone -c core.longpaths=true <your.url.here>

 


git config --global user.name "K,Rajkamal"
git config --global user.email "Rajkamal.k@sc.com"
git config --global user.password "2021@AuguRaj"

Pending things:
---------------


To raise PR From IntelliJ Idea
